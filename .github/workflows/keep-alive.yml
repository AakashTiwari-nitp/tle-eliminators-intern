name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes (UTC time)
  workflow_dispatch:  # Allow manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your Render backend
        run: curl -s https://tle-eliminators-intern.onrender.com/get-students
